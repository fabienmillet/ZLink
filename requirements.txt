PyQt6>=6.7.0
PyQt6-WebEngine>=6.7.0
python-mpv>=1.0.7
httpx>=0.27.0
streamlink>=8.4.0   # < 8.4.0 : CVE-2026-44353 (lecture de fichier local via manifeste HLS/DASH)
python-dotenv>=1.0.0
qtawesome>=1.3.0
pyqtgraph>=0.13.0
# Vérification des signatures Ed25519 des releases (core/updater.py).
# Sans elle, verify_signature() échouait à l'import et retournait False :
# une mise à jour signée aurait été rejetée comme non vérifiable.
pycryptodome>=3.20.0

# Stream Deck piloté en direct, sans logiciel Elgato (core/streamdeck_direct.py).
#
# Linux seulement : ailleurs, le boîtier appartient au logiciel d'Elgato, qui
# l'ouvre en exclusif — on passe alors par l'extension de streamdeck/, et ces
# deux paquets ne serviraient à rien.
#
# Il leur faut hidapi, qui est un paquet SYSTÈME et non Python :
# `pacman -S hidapi`, `apt install libhidapi-hidraw0`, `dnf install hidapi`.
# Sans lui l'énumération ne rend rien, ZLink le note et tourne sans.
streamdeck>=0.9.5 ; sys_platform == "linux"
Pillow>=10.1.0 ; sys_platform == "linux"
